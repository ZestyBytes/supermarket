node_modules/
dist/
*.local
.DS_Store

# Retailer endpoints, edited locally. Never contains the session itself.
retailer.config.json

# Requests copied out of DevTools contain the session cookie. Never commit them.
search.txt
basket.txt
add.txt
*.curl.txt
capture/
*.har
*.curl
*cookies*.json
*session*.json
.local/
.env*
!.env.example
test-results/
playwright-report/

# The seeded design canvas is 2.5MB of editor payload rebuilt from the
# .dc.html sources beside it; keep the sources, not the build.
.design/*.html
!.design/*.dc.html
browser-extension/app/
